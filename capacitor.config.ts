import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.mcpartlan.CapacitorGeolocation',
  appName: 'Capacitor Geolocation',
  webDir: 'out'
};

export default config;
